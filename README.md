Sources for the plugin `plugin.firebaseConfig`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.firebaseConfig" = {
            publisherId = "tech.scotth",
        },
    },
}
```
